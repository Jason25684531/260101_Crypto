# HighFreqQuant 交易系統開發日誌
**日期：2025年12月31日**

---

## 📊 專案概覽

**專案名稱：** HighFreqQuant - 高 Alpha、低風險量化交易系統  
**技術棧：** Python 3.11 + Docker + MySQL 8.0 + Redis + Flask + Streamlit + CCXT + VectorBT  
**目標：** 打造一個結合技術指標、鏈上數據、Kelly Criterion 的自動化交易系統

---

## ✅ 目前完成階段

### Phase 1: Infrastructure & Data Foundation（100% 完成）
**時間：** 2025年12月初  
**成果：**
- ✅ Docker 環境建置（5 個服務：app, db, cache, ngrok, dashboard）
- ✅ MySQL 8.0 + Redis 快取架構
- ✅ SQLAlchemy ORM 模型：`MarketData`（OHLCV）、`ChainMetric`（鏈上指標）
- ✅ 數據管道：`MarketFetcher`（異步 CCXT）+ `DataCleaner`（重採樣）
- ✅ 單元測試覆蓋：`tests/unit/test_fetcher.py`

**關鍵檔案：**
- `docker-compose.yml` - 服務編排
- `app/models/market.py` - OHLCV 資料表
- `app/core/data/fetcher.py` - 市場數據抓取器
- `app/core/data/cleaner.py` - 數據清洗邏輯

---

### Phase 1.5: Local MVP & Visualization（100% 完成）
**時間：** 2025年12月31日  
**成果：**
- ✅ Streamlit Dashboard 建置（端口 8501）
- ✅ VectorBT 回測引擎（RSI + Bollinger Bands 策略）
- ✅ Binance 數據種子腳本（支援 BTC/USDT, ETH/USDT）
- ✅ 三大功能頁面：市場數據、回測結果、交易信號

**新增檔案：**
```
app/dashboard/
  ├── app.py                    # Streamlit 主應用
  └── __init__.py
app/core/strategy/
  └── backtest.py               # VectorBT 回測引擎
app/core/data/
  └── fetcher.py               # 新增 BinanceFetcher 同步類
scripts/
  └── seed_data.py              # 數據初始化腳本
```

**功能說明：**

#### 📈 Tab 1: 市場數據
- K線圖（Candlestick Chart）
- 布林通道（Bollinger Bands）疊加
- RSI 技術指標（14 週期）

#### 📊 Tab 2: 回測結果
- 資金曲線圖（Equity Curve）
- 績效指標卡片：
  - 總收益率（Total Return）
  - 夏普比率（Sharpe Ratio）
  - 最大回撤（Max Drawdown）
  - 勝率（Win Rate）

#### 🎯 Tab 3: 交易信號
- Kelly Criterion 建議倉位
- 市場恐慌指數（Panic Score）
- 當前持倉建議

---

### Phase 2: Strategy Engine & Risk Control（100% 完成）
**時間：** 2025年12月中旬  
**成果：**
- ✅ Alpha 因子庫：RSI、Bollinger Width、On-Chain Z-Score
- ✅ Kelly Criterion 動態倉位計算器
- ✅ 策略引擎：`CompositeScore` 綜合評分系統
- ✅ 單元測試覆蓋：`tests/unit/test_kelly.py`, `tests/unit/test_factors.py`

**關鍵檔案：**
- `app/core/strategy/factors.py` - Alpha 因子計算
- `app/core/strategy/engine.py` - 策略引擎
- `app/core/risk/kelly.py` - Kelly Criterion

---

### Phase 3: Execution & LineBot（50% 完成）
**時間：** 2025年12月底  
**成果：**
- ✅ Flask App Factory 架構
- ✅ TradeExecutor（止盈 10-20%、止損 5%）
- ✅ LINE Bot 通知器（TradingNotifier）
- ⚠️ **待整合：** Ngrok 公網訪問 + Flask 路由 + Webhook

**已實作檔案：**
- `app/__init__.py` - Flask App Factory
- `app/api/routes.py` - REST API 端點
- `app/core/execution/trader.py` - 交易執行器
- `app/core/execution/notifier.py` - LINE Bot 通知

**待完成任務：**
- [ ] Ngrok 服務整合到 docker-compose
- [ ] LINE Bot Webhook 端點測試
- [ ] 命令處理器（`/status`, `/stop`, `/panic`）

---

## 🏗️ 當前系統架構

### 🐳 Docker 服務架構
```
┌─────────────────────────────────────────────────┐
│             Docker Compose 編排                  │
├─────────────────────────────────────────────────┤
│  app:5000        │ Flask 後端 + 策略引擎         │
│  dashboard:8501  │ Streamlit 視覺化儀表板        │
│  db:3307         │ MySQL 8.0 資料庫             │
│  cache:6379      │ Redis 快取層                 │
│  ngrok:4040      │ 公網隧道控制台               │
└─────────────────────────────────────────────────┘
```

### 📂 核心模組分層

```
┌───────────────────────────────────────────────┐
│          Presentation Layer (呈現層)          │
│  ┌─────────────┐      ┌─────────────┐        │
│  │  Flask API  │      │  Streamlit  │        │
│  │  (routes)   │      │  Dashboard  │        │
│  └─────────────┘      └─────────────┘        │
└───────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────┐
│         Business Logic Layer (業務層)         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Strategy │  │   Risk   │  │ Execution│   │
│  │  Engine  │  │  Kelly   │  │  Trader  │   │
│  └──────────┘  └──────────┘  └──────────┘   │
└───────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────┐
│           Data Layer (數據層)                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Fetcher  │  │ Cleaner  │  │ Backtest │   │
│  │  (CCXT)  │  │(Resample)│  │(VectorBT)│   │
│  └──────────┘  └──────────┘  └──────────┘   │
└───────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────┐
│        Persistence Layer (持久層)             │
│         MySQL 8.0  +  Redis Cache            │
└───────────────────────────────────────────────┘
```

### 🔄 數據流程
```
Binance API → MarketFetcher → MySQL (OHLCV)
                                   ↓
                            Alpha Factors
                                   ↓
                          Kelly Position Size
                                   ↓
                             TradeExecutor
                                   ↓
                          LINE Bot Notification
```

---

## 📁 完整檔案架構

```
d:\01_Project\260101_Crypto/
├── app/                          # 應用核心
│   ├── api/                      # API 層
│   │   ├── routes.py            # Flask 路由（/webhook, /health）
│   │   └── __init__.py
│   ├── core/                     # 核心業務邏輯
│   │   ├── data/                # 數據層
│   │   │   ├── fetcher.py       # CCXT 抓取器（含 BinanceFetcher）
│   │   │   └── cleaner.py       # 數據清洗器
│   │   ├── execution/           # 執行層
│   │   │   ├── trader.py        # 交易執行器（止盈止損邏輯）
│   │   │   └── notifier.py      # LINE Bot 通知器
│   │   ├── risk/                # 風險管理
│   │   │   └── kelly.py         # Kelly Criterion
│   │   └── strategy/            # 策略引擎
│   │       ├── factors.py       # Alpha 因子（RSI, BBands, OnChain）
│   │       ├── backtest.py      # VectorBT 回測引擎 ⭐ NEW
│   │       └── engine.py        # 策略引擎主邏輯
│   ├── dashboard/               # Streamlit 儀表板 ⭐ NEW
│   │   ├── app.py              # 主儀表板（3 個 Tab）
│   │   └── __init__.py
│   ├── models/                   # SQLAlchemy 模型
│   │   ├── market.py            # OHLCV 資料表
│   │   ├── onchain.py           # ChainMetric 資料表
│   │   └── __init__.py
│   ├── extensions.py            # Flask 擴展（db, migrate）
│   └── __init__.py              # App Factory
│
├── tests/                        # 測試套件
│   ├── unit/                    # 單元測試
│   │   ├── test_fetcher.py      # 數據抓取測試
│   │   ├── test_kelly.py        # Kelly Criterion 測試
│   │   ├── test_factors.py      # Alpha 因子測試
│   │   └── test_trader.py       # 交易執行器測試
│   ├── integration/             # 整合測試（保留）
│   └── conftest.py              # Pytest 配置
│
├── scripts/                      # 工具腳本
│   ├── seed_data.py             # 數據種子腳本 ⭐ NEW
│   └── verify_system.py         # 統一系統驗證腳本
│
├── openspec/                     # OpenSpec 規範
│   ├── AGENTS.md
│   ├── project.md
│   └── changes/
│       └── master-roadmap/
│           └── task.md          # Phase 1-4 任務清單
│
├── docker-compose.yml            # Docker 服務編排（5 個服務）
├── Dockerfile                    # Python 3.11 映像
├── requirements.txt              # Python 依賴（含 streamlit, vectorbt）
├── .env.example                  # 環境變數範本
├── .gitignore                    # Git 忽略規則
└── README.md                     # 專案說明文件
```

---

## 🚀 使用流程

### 1️⃣ 環境準備
```powershell
# 複製環境變數範本
cp .env.example .env

# 編輯 .env，填入 API Key
# LINE_CHANNEL_SECRET=your_secret
# LINE_CHANNEL_ACCESS_TOKEN=your_token
# BINANCE_API_KEY=your_key
# BINANCE_SECRET_KEY=your_secret
```

### 2️⃣ 啟動服務
```powershell
# 建置並啟動所有服務
docker-compose up -d --build

# 查看服務狀態
docker-compose ps
```

### 3️⃣ 初始化數據
```powershell
# 抓取 BTC/USDT 最近 30 天數據
docker-compose exec app python scripts/seed_data.py --symbol BTC/USDT --days 30

# 抓取多個交易對
docker-compose exec app python scripts/seed_data.py --symbol ETH/USDT --days 30
```

### 4️⃣ 訪問儀表板
開啟瀏覽器前往：**http://localhost:8501**

---

## 🎯 未來展望與下一步

### 🔥 Phase 3 完成計劃（優先級：高）

#### 任務清單
1. **Ngrok 整合**（1-2 小時）
   - [ ] 修改 `docker-compose.yml`，確保 ngrok 服務正確連接到 Flask app
   - [ ] 測試公網 URL 訪問 Flask `/health` 端點
   - [ ] 在 LINE Developers Console 設定 Webhook URL

2. **LINE Bot 命令處理器**（2-3 小時）
   - [ ] 實作 `/status` 命令：回報當前持倉、績效、Kelly 倉位
   - [ ] 實作 `/stop` 命令：緊急停止所有交易
   - [ ] 實作 `/panic` 命令：查詢市場恐慌指數
   - [ ] 實作 `/backtest` 命令：觸發回測並回傳結果圖片

3. **Trade Executor 整合測試**（2-3 小時）
   - [ ] 模擬環境測試止盈止損邏輯
   - [ ] 實作 Pre-Flight Check（檢查 PanicScore 是否 < 閾值）
   - [ ] 測試 LINE Bot 即時通知（訂單成功/失敗/觸發止損）

---

### 🌟 Phase 4: 安全與部署（優先級：中）

#### 任務清單
1. **安全防護**（3-4 小時）
   - [ ] 整合 `bandit` 靜態程式碼掃描
   - [ ] 整合 `safety` 依賴漏洞檢測
   - [ ] 實作 `scripts/watchdog.py`（冷錢包自動掃帳功能）
   - [ ] API Key 加密儲存（使用 `cryptography` 庫）

2. **生產環境部署**（4-6 小時）
   - [ ] 配置 Nginx 反向代理（含 rate limiting）
   - [ ] 設定 `fail2ban` 防暴力破解
   - [ ] 配置 HTTPS（Let's Encrypt）
   - [ ] 設定 Systemd 守護進程
   - [ ] 實作健康檢查端點（Kubernetes Readiness Probe）

---

### 💡 功能擴展計劃（優先級：低）

1. **多交易所支援**
   - 新增 OKX, Bybit 數據源
   - 實作跨交易所套利邏輯

2. **鏈上數據整合**
   - Glassnode API 整合（Exchange Netflow, SOPR）
   - 鏈上 Z-Score 計算

3. **進階回測功能**
   - 支援多策略組合回測
   - Walk-Forward Analysis
   - Monte Carlo Simulation

4. **機器學習整合**
   - LightGBM 預測模型
   - 特徵工程自動化
   - 超參數自動優化

---

## 📝 開發紀錄

### 2025年12月31日 - Phase 1.5 完成
- ✅ 建置 Streamlit Dashboard（3 個功能頁面）
- ✅ 實作 VectorBT 回測引擎
- ✅ 新增 BinanceFetcher 同步數據抓取器
- ✅ 建立 `seed_data.py` 數據種子腳本
- ✅ 更新 `docker-compose.yml` 增加 dashboard 服務
- ✅ 更新 `requirements.txt` 增加 streamlit, plotly, vectorbt

### 2025年12月中旬 - Phase 1 & 2 完成
- ✅ Docker 環境完整建置
- ✅ MySQL + Redis 架構搭建
- ✅ SQLAlchemy ORM 模型定義
- ✅ Alpha 因子庫實作
- ✅ Kelly Criterion 風險管理
- ✅ 單元測試覆蓋率達 80%

---

## 🔧 技術債務

### 待優化項目
1. **測試覆蓋率**
   - 目前覆蓋率約 75%
   - 目標：達到 90% 以上

2. **日誌系統**
   - 統一日誌格式（JSON Logging）
   - 整合 ELK Stack（Elasticsearch, Logstash, Kibana）

3. **監控告警**
   - Prometheus + Grafana 整合
   - 異常交易自動告警

4. **文檔完善**
   - API 文檔（Swagger/OpenAPI）
   - 架構圖（使用 Mermaid）
   - 部署文檔（SOP）

---

## 🎓 學習與反思

### 技術亮點
1. **App Factory 模式** - Flask 應用的可測試性大幅提升
2. **VectorBT 回測** - 向量化計算效能遠超傳統 for-loop
3. **Kelly Criterion** - 動態倉位管理有效控制風險
4. **Streamlit 快速原型** - 30 分鐘內建立可視化界面

### 遇到的挑戰
1. **Docker 卷宗權限問題** - 解決方式：使用 `chown` 調整權限
2. **CCXT 異步抓取** - 解決方式：同時實作同步版本 `BinanceFetcher`
3. **VectorBT 相依性** - 解決方式：提供 pandas 降級方案

### 最佳實踐
- **先寫測試，後寫實作** - TDD 大幅減少 debug 時間
- **模組化設計** - 每個模組職責單一，易於維護
- **環境變數管理** - 使用 `.env` 避免硬編碼敏感資訊

---

## 📌 總結

### 專案進度概覽
- ✅ **Phase 1**: Infrastructure & Data Foundation（100%）
- ✅ **Phase 1.5**: Local MVP & Visualization（100%）
- ✅ **Phase 2**: Strategy Engine & Risk Control（100%）
- ⚠️ **Phase 3**: Execution & LineBot（50%）
- ⏳ **Phase 4**: Security & Deployment（0%）

### 關鍵成就
1. 完整的 Docker 微服務架構
2. 可視化儀表板（即時查看市場數據與回測結果）
3. 完善的單元測試覆蓋
4. 模組化的程式碼設計

### 下一步行動（建議優先順序）
1. **立即執行**：完成 Phase 3 的 Ngrok + LINE Bot 整合
2. **本週內**：實作 Trade Executor 整合測試
3. **本月內**：啟動 Phase 4 安全防護與部署準備

---

## 📞 聯絡方式

如有問題或建議，請參考：
- 專案文檔：[openspec/project.md](openspec/project.md)
- 任務清單：[openspec/changes/master-roadmap/task.md](openspec/changes/master-roadmap/task.md)
- 架構說明：[README.md](README.md)

---

**最後更新：** 2025年12月31日  
**維護者：** HighFreqQuant Team
